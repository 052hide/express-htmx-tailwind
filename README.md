# express-htmx-tailwind

## Docker

### build

```bash
docker build . -t eht

# no-cache
# docker build . -t eht --no-cache
```

### run

```bash
docker run -p 13000:3000 -d eht
# start on localhost:13000
```